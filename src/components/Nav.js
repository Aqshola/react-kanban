import Button from './Button';

export default function Nav(params) {
  return (
    <nav className="p-5  items-center w-full">
      <h1 className="text-xl font-bold text-center mx-auto">
        Simple Kanban Board
      </h1>
    </nav>
  );
}
